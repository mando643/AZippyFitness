@model A10.Models.ClassEnrollee

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>ClassEnrollee</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="enrolledRegistrationId" />
            <div class="form-group">
                <label asp-for="classSessionId" class="control-label"></label>
                <select asp-for="classSessionId" class="form-control" asp-items="ViewBag.classSessionId"></select>
                <span asp-validation-for="classSessionId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="memberId" class="control-label"></label>
                <select asp-for="memberId" class="form-control" asp-items="ViewBag.memberId"></select>
                <span asp-validation-for="memberId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
